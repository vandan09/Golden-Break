/// <summary>
/// Every player-facing string lives here — no hardcoded UI text in
/// controller/screen code. Populated as each screen is built (Phase 4-5).
/// </summary>
public static class Strings
{
}
